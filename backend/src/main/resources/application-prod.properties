# Database Configuration for Production
db.default.url=jdbc:postgresql://${DB_HOST:postgres}:${DB_PORT:5432}/${DB_NAME:parking_db}
spring.datasource.url=${SPRING_DATASOURCE_URL:${db.default.url}}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:${DB_USER:parking_user}}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:${DB_PASSWORD:ParkingDB@2024!}}

# Flyway Migration Validation
spring.flyway.validate-on-migrate=false

# JWT Configuration (15 Minutes Expiration for Prod)
app.jwtSecret=${JWT_SECRET:ChangeThisSecretToAStrongValueSuperLongKeyForSWP391ParkingProject2026Compliant}
app.jwtExpirationMs=900000

# VNPay Credentials
vnp.tmncode=${VNP_TMN_CODE:3WFSAH6C}
vnp.hashsecret=${VNP_HASH_SECRET:W5QQCZ0C958UEDBFXCA439X0ET0XKM5A}

# MoMo Credentials
momo.secret-key=${MOMO_SECRET_KEY:at67q66hr7572013614151515151}
momo.access-key=${MOMO_ACCESS_KEY:klm0566hr757201}

# Bind to all IPv6 and IPv4 interfaces for Railway Private Networking compatibility
server.address=::
